# Stripe API Keys
# Get your keys from: https://dashboard.stripe.com/test/apikeys

# Stripe Secret Key (KEEP THIS SECRET - Never commit to git)
STRIPE_SECRET_KEY=sk_test_YOUR_SECRET_KEY_HERE

# Site URL (for production, use your actual domain)
URL=http://localhost:8888

# Instructions:
# 1. Copy this file to .env (which is gitignored)
# 2. Replace the placeholder values with your actual Stripe keys
# 3. For local development, use test mode keys (they start with sk_test_ and pk_test_)
# 4. For production, use live mode keys (they start with sk_live_ and pk_live_)
# 5. Update the publishable key in js/stripe.js as well
